import { Meta,  Source } from '@storybook/blocks';

<Meta title="api/initAxiosInterceptor" />

# initAxiosInterceptor

<Source code={`
  initAxiosInterceptor = <U,>(
    store: ToolkitStore,
    apiUrl: string,
    publicApi: string,
    loginEnpoint: string,
    refreshTokenEndPoint: string,
  )
`} />
